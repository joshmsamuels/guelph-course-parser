{
  "name": "guelph-course-parser",
  "version": "1.0.0",
  "description": "Grabs course information from the University of Guelph's website and converts it to JSON and writes the JSON to a file",
  "private": true,
  "scripts": {
    "build": "webpack",
    "start": "echo \"Error: no start specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Samuels",
  "license": "ISC",
  "dependencies": {
    "fetch-cookie": "^0.11.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "webpack": "^4",
    "webpack-cli": "^4.2.0"
  }
}
